# thg Documentation

Reference material for `thg` and the Things 3 data it works with.

- **[database.md](database.md)** — The Things 3 SQLite database: location, schema,
  date encoding, and the queries `thg` uses to read tasks.
- **[url-scheme.md](url-scheme.md)** — The full `things:///` URL scheme reference.
  `thg` uses this scheme for all writes (add, update, complete).

For installation and command usage, see the [main README](../README.md).
For contributor notes and architecture, see [CLAUDE.md](../CLAUDE.md).
